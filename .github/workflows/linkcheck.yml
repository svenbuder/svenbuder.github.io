name: Link Check

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  linkcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Check links
        uses: lycheeverse/lychee-action@v2
        with:
          args: --exclude 'https://www\.arc\.gov\.au/.*' --accept 200,204,301,302,403,999 --verbose --no-progress "**/*.md" "**/*.html"
